import React from "react";
import { SliderData } from "./SliderData";

function ImgArea() {
  return (
    // <div className="main-image-area">
    //   {SliderData.map((slide, index) => {
    //     return <img key={slide.id} src={slide.image} alt="garden" />;
    //   })}
    // </div>
    // see source for slider carousel - Stopped development as coulded seem to get pictures uploaded from its file source https://www.youtube.com/watch?v=l1MYfu5YWHc

    // If you go to Section One ....ParaDescriptionTex.jsx we just added a font awesom icon link to the instagram page which was far easier for clients to see updated garded results and follow the account - instead of have developer having to not only upload new results picures on instagram but also on the website. Now there is a simple link to bridge the two.
    <div className="main-image-area"></div>
  );
}

export default ImgArea;
